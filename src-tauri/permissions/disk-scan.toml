[[permission]]
identifier = "allow-scan"
description = "Allows the main window to list drives, scan directories, cancel scans, and reveal selected paths."
commands.allow = [
  "list_roots",
  "start_scan",
  "cancel_scan",
  "reveal_path"
]

