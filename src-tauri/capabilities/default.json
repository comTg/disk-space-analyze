{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for disk scanning.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-scan"]
}

